import React from "react";
import { Outlet } from 'react-router-dom';

export default function SignIn() {
    return (
        <div class="container" >
            <Outlet />
        </div>
    );

}

